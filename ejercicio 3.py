# Definimos una variable de control
ejecutar = True

# Bucle Do While
while ejecutar:
    # Aquí iría el código que queremos que se ejecute al menos una vez
    print("Este código se ejecuta al menos una vez")
    
    # Modificamos la variable de control para salir del bucle
    ejecutar = False